#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
  *main - prints only using putchar
  *Return: Always (Success)
 */
int main(void)
{
int y;
for (y = 0; y < 9; y++)
putchar(y);
putchar('\n');
return (0);
}
